OPENAI_API_KEY=sk-your-api-key-here
PORT=3000
TARGET_LANGUAGE=es
# gated (default): only speech is streamed to OpenAI (cheaper).
# continuous: stream silence too — keeps the translation voice more stable.
VAD_MODE=gated
